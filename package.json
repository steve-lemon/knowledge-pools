{
  "name": "knowledge-pools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Implementation-near specs and SummaryAgent prototype for Knowledge Pools.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "summary": "node dist/cli/summary.js",
    "dev:summary": "tsx src/cli/summary.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
