# Knowledge Pools Summary Fixture

Knowledge Pools is an implementation-near specification project.

It keeps runtime sample code intentionally small so the architecture can be validated before broad implementation starts.

The first sample agent is SummaryAgent, which reads a local path through a storage-backed tool and summarizes the bounded input through an LLM gateway.
